(function(){function e(s){return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;')}function i(t){return t.replace(/!\[([^\]]*)\]\(([^)]+)\)/g,'<img loading="lazy" decoding="async" alt="$1" src="$2">').replace(/\[([^\]]+)\]\(([^)]+)\)/g,'<a href="$2" target="_blank" rel="noopener noreferrer">$1</a>').replace(/\*\*([^*]+)\*\*/g,'<strong>$1</strong>').replace(/__([^_]+)__/g,'<strong>$1</strong>').replace(/\*([^*]+)\*/g,'<em>$1</em>').replace(/_([^_]+)_/g,'<em>$1</em>').replace(/`([^`]+)`/g,'<code>$1</code>')}function table(lines){const rows=lines.map(l=>l.trim()).filter(Boolean),cells=r=>r.split('|').map(c=>c.trim()).filter((c,idx,a)=>!(idx===0&&c==='')&&!(idx===a.length-1&&c===''));if(rows.length<2)return rows.join('\n');const h=cells(rows[0]),d=cells(rows[1]);if(!d.every(c=>/^:?-{3,}:?$/.test(c)))return rows.join('\n');const body=rows.slice(2).map(r=>cells(r));return `<table><thead><tr>${h.map(x=>`<th>${i(x)}</th>`).join('')}</tr></thead><tbody>${body.map(r=>`<tr>${r.map(c=>`<td>${i(c)}</td>`).join('')}</tr>`).join('')}</tbody></table>`}function parse(md){const code=[];let text=md.replace(/\r\n?/g,'\n').replace(/```([\s\S]*?)```/g,(_,c)=>{const t=`@@CODE${code.length}@@`;code.push(`<pre><code>${e(c.trim())}</code></pre>`);return t});const html=text.split(/\n\n+/).map(chunk=>{const t=chunk.trim();if(!t)return '';if(/^@@CODE\d+@@$/.test(t))return t;if(/^(\|.+\|\n?)+$/.test(t)&&t.includes('\n'))return table(t.split('\n'));if(/^#{1,6}\s+/.test(t)){const l=t.match(/^#+/)[0].length;return `<h${l}>${i(t.replace(/^#{1,6}\s+/,''))}</h${l}>`}if(/^>\s+/.test(t))return `<blockquote>${i(t.replace(/^>\s?/,'').replace(/\n>\s?/g,'<br>'))}</blockquote>`;if(/^---+$/.test(t))return '<hr>';if(/^(?:- |\* |\d+\. )/m.test(t)){const lines=t.split('\n'),tag=/^\d+\. /.test(lines[0])?'ol':'ul';return `<${tag}>${lines.map(l=>`<li>${i(l.replace(/^(- |\* |\d+\. )/,''))}</li>`).join('')}</${tag}>`}return `<p>${i(t.replace(/\n/g,'<br>'))}</p>`}).join('\n');return html.replace(/@@CODE(\d+)@@/g,(_,n)=>code[Number(n)])}window.MarkdownEngine={parse}})();
